/** Linear connector (M6). Stub — replaced by the connector implementation. */
import type { Tool } from "@/agent/tools/types";
export const linearTools: Tool[] = [];
export async function syncLinear(_userId: string): Promise<number> {
  return 0;
}
