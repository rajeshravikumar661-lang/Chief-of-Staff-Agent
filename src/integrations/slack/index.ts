/** Slack connector (M6). Stub — replaced by the connector implementation. */
import type { Tool } from "@/agent/tools/types";
export const slackTools: Tool[] = [];
export async function syncSlack(_userId: string): Promise<number> {
  return 0;
}
